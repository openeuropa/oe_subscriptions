{#
/**
 * @file
 * Default theme implementation for anonymous subscribe link.
 * Available variables:
 * - link: The link text.
 * - url: The link destination.
 * - attributes: associative array containing the properties of the element.
 */
#}
{% apply spaceless %}
  {# Set set URL from variables #}
  <a{{ attributes.addClass(['button', 'button--small']) }}>{{ title }}</a>
{% endapply %}
